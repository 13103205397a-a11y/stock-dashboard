# 股市看板最终验收报告

日期：2026-07-29  
范围：退休功能删除、数据安全、刷新与发布链路、桌面/手机交互、性能和可维护性  
结论：**通过。** 本轮列出的 P1/P2/P3 问题均已修复或随退休功能彻底删除；未发现 P0/P1 阻断项。

## 1. 功能与数据边界

- 正式保留 6 个视图：首页、巨头核心、逻辑链、外围热点、今日热点事件、周末发酵。
- 持仓、机会清单、AI 复盘、产业链涨价、产业雷达、市场异动页、资金流向页、今日热点页和新闻聚合页的 DOM、路由、搜索跳转、数据脚本、生成器及提示词均已删除。
- 退休或未知 hash 会使用 `replaceState` 规范化为 `#home`。
- `active_modules.json` 成为公开构建、数据契约、新鲜度、Hermes 发布和部署后探针的唯一模块清单。
- 历史 `review` 字段仅继续展示，不再触发已停用任务的新鲜度门禁。

## 2. 安全与可靠性

- 持仓 API、后台线程和专属前端已删除；`/api/portfolio` 与 `portfolio.json` 均返回 404。
- 本地服务器采用静态白名单，仓库配置和未公开文件无法通过 HTTP 读取。
- 所有 `/api/*` 校验实际监听端口对应的 localhost/127.0.0.1 Host；写请求继续校验 Origin。
- 刷新端口可通过 `STOCK_DASHBOARD_PORT` 配置，并拒绝非法端口。
- Hermes 只发布本轮明确成功的候选文件；候选早于远端，或时间相同但内容冲突时会拒绝覆盖。
- Mac App 仅在刷新进程成功退出后 reload，失败会保留错误状态。

## 3. 发布与自动化

- Pages 在上传产物前依次执行数据校验、单元/集成测试和完整 Playwright E2E；E2E 失败会阻断部署。
- 部署后探针与 `scripts/build_site.py` 读取同一真实文件清单，不再探测已退休资源。
- 行情工作流只写入 `data.js`、`meta.js` 和 `market.js`，不再触碰 Hermes 模块。
- 新鲜度检查严格拒绝非法日期和未来日期。
- Playwright 默认使用隔离端口 8791，除非显式指定，否则不会复用另一工作树的服务。

## 4. 移动端、可访问性与视觉

- 手机底栏为 3 个等宽按钮，不再沿用 5 列布局。
- “更多”同步 `aria-controls` / `aria-expanded`，支持 Enter 展开、Escape 关闭和焦点恢复。
- 全局搜索支持方向键、Enter、Escape；详情抽屉具备 dialog 语义、焦点圈定与关闭后焦点恢复。
- 六个活动视图在桌面和手机均无页面级横向溢出。
- 外围热点正文未发现 `�` 替换字符、内部生成字段或残缺括号。
- 内置浏览器复验控制台 warning/error 为 0。

![最终首页](qa/final-hardening-home.png)

## 5. 性能与维护性

- 发布字体由 4 个字重缩至 Regular + Bold：19,876,460 → 9,939,728 bytes，减少约 50%。
- 三份 CSS 从 8,399 行降至 6,704 行，体积减少约 24.5%，选择器减少约 29%。
- 公开构建从 24 个文件收敛为 16 个文件，构建体积约 11 MB。
- 退休模块的数据文件、报告页、提示词、生成脚本和专属样式已删除，避免未来误维护和空跑。

## 6. 最终验证

- `npm test`：8 个 Node 信号测试、3 个交易日历测试、39 个 Python 单元/集成测试全部通过。
- `node scripts/validate_data.js`：7 个活动模块、47 只股票通过。
- `node scripts/check_freshness.js --strict`：7/7 通过（含 A 股法定休市交易日口径）。
- Playwright：35 passed，1 个 desktop 不适用手机菜单用例按预期跳过。
- 公开构建：退休和私有资源均未进入产物。
- 前端对历史快照中的退休数据源文件名做即时兜底清洗（如 `newsall.js` → 公开资讯）。
- 内置浏览器：6/6 页面可见、无乱码、无横向溢出、控制台 0 条警告或错误。

## 7. 验收结论

项目当前达到本轮发布条件。VoiceOver 的真实人工朗读体验仍属于设备级人工验收，不影响本轮代码与浏览器自动化结论。
